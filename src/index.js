export const name = 'dsh-markdown-link-preview'

// This plugin runs entirely in the DSH Web client. The empty host half makes
// it safe to mount through the normal DSH bundle mechanism.
export function apply() {}
